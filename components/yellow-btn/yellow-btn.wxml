<view class="yellow">
  <view class="yellow-text" wx:if="{{active==0}}">{{title}}</view>
  <view class="yellow-textActive" wx:if="{{active==1}}">{{title}}</view>
  <view class="{{active==1? 'yellow-bg':''}}"></view>
  
</view>